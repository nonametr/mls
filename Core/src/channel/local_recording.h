#ifndef LOCAL_RECORDING_H
#define LOCAL_RECORDING_H
/*
#include "ichannel.h"

class LocalRecording : public IChannel
{
public:
	static IChannel	*create();

	virtual void start();
	virtual void stop();
	virtual bool setup(map<string, string> init_ops);
	vector<string> getConfigFields();

	virtual ~LocalRecording(void){};
private:
	LocalRecording(void){};
public slots:
	virtual void onFrameReady(char *buf, unsigned int buf_len);
};
*/
#endif

